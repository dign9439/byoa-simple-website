{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A simple static site with blog functionality using Markdown.",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "http-server ./public -p 3000 -o",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
